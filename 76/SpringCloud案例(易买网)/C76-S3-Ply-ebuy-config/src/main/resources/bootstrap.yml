spring:
  application:
    name: ebuy-config  # 应用名称
  cloud:
     config:
        server:
          git:
            # 配置文件所在仓库
            # uri: https://github.com/306529917/ebuy-config
            # 登录账号
            # username: github
            # 登录密码
            # password: github
            # 配置文件分支
            # default-label: master
            # 配置文件所在根目录
            search-paths: 76/SpringCloud案例(易买网)/C76-S3-Ply-ebuy-config/src/main/resources/config
            uri: file:///e:/dev/github-llsok-ycdemo
            #search-paths: 76/C76-S3-Ply-ebuy-config/src/main/resources/config