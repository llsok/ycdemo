spring:
  application:
      name: ebuy-order
  cloud:
     config:
       uri: http://localhost:8080
       label: master

---
spring:
   profiles: order8005
   cloud:
     config:
       profile: order8005